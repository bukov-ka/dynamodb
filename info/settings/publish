more logs
dotnet publish src\MyProject -o ..\..\publish /v:n

the properties of the web.config "Copy to Output Directory" to "Always"

Otherwise no copy on linux


package.json
    "build": "ng build",
    "build:Debug": "ng build --dev --base-href /",
    "build:Release": "ng build --prod --base-href=/aws-test/aws-publish/ClientApp/dist/ --deploy-url=/aws-test/aws-publish/ClientApp/dist/",
    "build:ssr": "ng run WebConjugacion:server:dev",
    "test": "ng test",
